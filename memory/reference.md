# dashboard-jorge — Memory Index

Flat index of every memory file. Read this first on every task.

- `google-ads-integration.md` — **Google Ads API direta (fase 1, pronta para ligar)**: `server/integrations/googleAds/` (OAuth c/ refresh token cifrado, cliente REST só leitura, sync horária/noturna/mensal retomável com lock e validação, fonte única `adMetrics` API>legado por dia, `marketingStats` com reservas por data de criação atribuídas vs sem atribuição, `attribution` por gclid/utm no originUrl), migração 0063, página `/integracoes/google-ads`, Marketing com cobertura/última recolha, gasto ≠ orçamento. Inclui o que o Jorge tem de fazer na Google (envs, redirect URI, developer token) e a fase E por fazer.
- `expenses-financial-control.md` — Despesas (set 2026): fase 1 FEITA — regra ÚNICA de âmbito/filtros (`server/expenseScope.ts`: cidade inclui descendentes via `resolveProjectIds`, visibilidade igual em lista/detalhe/totais/Excel/documento), `paidAt` preservado, canceladas fora dos totais (`shared/expenseTotals`), fim do intervalo inclusivo (`shared/expensePeriods`), URL assinada dos comprovativos (`expenses.documentUrl`/`storagePresignGet`), PDF no detalhe, recorrentes no cron com lock+UNIQUE (`server/expenseRecurring.ts`), histórico `expense_events`, NIF/nº doc/pago-por + aviso de duplicado. **Migração 0062** cria também `finance_accounts`, `expense_payments`, `expense_budgets`, `finance_import_batches` (vazias) e `approvalStatus='legacy'` — base para aprovações, pagamentos parciais/reembolsos, orçamentos e a IMPORTAÇÃO da caixa (que vive noutra app). Inclui o contrato da importação e os pendentes de infra (S3 privado p/ `invoices/`, charset).
- `finance-engine.md` — **Motor financeiro ÚNICO** (`server/finance/`: `rules.ts` puro + `engine.ts` + `compat.ts`; `legacy.ts` só p/ paridade): Faturação e Anual passam a usar a MESMA fórmula (receita CHECKED_OUT s/IVA − despesas s/IVA − pessoal c/ histórico, provisões e variável − TSU − equipa do dia só extras c/ saída antecipada e cidade − comissões como CUSTO; marketing FORA; pendentes = informação; custos sem centro = "Por atribuir"); gráfico ao dia de calendário = cartões ao cêntimo; `quality` com conflitos/lacunas; `requireFinanceTotals` também no Anual/diagnóstico. Tabela de paridade jun–set 2026 e razões de cada diferença; `scripts/finance-parity.ts`. Fases 2–4 por fazer.
- `identity-by-email.md` — **uma pessoa = um email** (2026-09-10: utilizador criado/ligado para cada ficha com email, agentes Multipark anexados por email, `identityReconcile.ts` + scripts `identity-audit`/`identity-reconcile`, migração 0066 users.email UNIQUE): como a identidade é resolvida entre `users` (login Google ou conta criada pelo backoffice) e `employees` (ficha, email opcional), o módulo canónico `server/identity.ts` (findUserByEmail / findEmployeeByEmail / findOrCreateExtraByEmail / adoptPlaceholderAccountByEmail), a mensagem ÚNICA de recusa de acesso `ACCESS_DENIED_MSG` + gate de `isActive` no login e em cada pedido, a ingestão do website multidriver sem criar duplicados, o normalizador de telefone tolerante a texto livre (`shared/phone.ts`), a tabela de extras que voltou a listar todos os ativos, e a ferramenta dry-run de fusão de extras duplicados (`server/mergeDuplicateExtras.ts` + `POST /api/v1/admin/merge-duplicate-extras`). Sem migração. ⚠️ contas com `isActive = 0` passam a ser recusadas.
- `profile-photo-upload.md` — collaborator profile photo: `ProfilePhotoPrompt` dialog (camera **+ upload from computer**, JPEG re-encode at 1280px/q0.85), `CameraCapture` selfie widget, and the `rh.uploadMyPhoto`/`rh.uploadPhoto` contract. Documents the REAL payload ceiling (~4.5MB Vercel serverless body cap, not the `express.json` 50mb) and that the backend validates neither mime nor size. Flags two open debts: `CameraCapture`'s file-picker button that was never rendered (PDA clock-in dead end) and the missing server-side mime/size guard.
- `storage-backends-s3.md` — `server/storage.ts`, o único ponto de storage da app: precedência **S3 (4 envs `AWS_S3_*`) → Vercel Blob (`BLOB_READ_WRITE_TOKEN`) → `./uploads` local** (erro alto no Vercel), bucket `dashboard-multipark-bucket` LIVE e verificado E2E (`scripts/verify-s3-storage.ts`, 9/9), contrato `storagePut`/`storageGet`/`storageDelete` inalterado + novo `storagePresignPut` (PUT assinado, 60s, **S3-only, sem rota ligada** — groundwork p/ vídeos, que não cabem no teto de ~4.5MB do body das funções Vercel). Especificidades S3: o utilizador IAM **não pode listar** (só `HeadObject` como sonda de existência), URL pública construída (`S3_PUBLIC_BASE_URL` ou derivada), `normalizeKey` aceita URL ou key e **não** corta o prefixo `uploads/`, SDK carregado por `import()` dinâmico (sem envs AWS = comportamento idêntico ao anterior). ⚠️ **RULE de nomes**: as credenciais do dashboard têm prefixo `AWS_S3_` (`AWS_S3_ACCESS_KEY` / `AWS_S3_SECRET_ACCESS_KEY`) porque o **Vercel reserva** os nomes sem prefixo; resolução `AWS_S3_* || AWS_*` (fallback legado) e o gate dos 4 corre sobre os valores RESOLVIDOS — divergência só do dashboard, `multipark`/`be-multipark` mantêm a convenção antiga. ⚠️ mudar de backend **não migra** os ficheiros já gravados; os 2 únicos `storageDelete` (expenses update/delete em `routers.ts`) passam `key || url` e deixam órfão um blob antigo quando o S3 está ativo. Sem migração de BD.
- `sync-runners-topology.md` — where the booking sync actually runs: Railway long-lived `setInterval` (15min) vs GitHub Actions cron hitting the Vercel HTTP endpoints. Dual-runner analysis + evidence.
- `employee-city-derivation.md` — cidade de um colaborador (Lisboa/Porto/Faro) **sem coluna de cidade em `employees`**: vocabulário partilhado `shared/city.ts` (`matchCityKey` conservador — "Portimão" nunca vira "Porto") + `server/employeeCity.ts` (árvore de `projects` `level='city'` → `driver_applications.city` → `employees.address`; "sem cidade" é um estado real e filtrável). Primeiro consumidor: filtro de cidade na tabela "Disponibilidade dos extras", que compõe com o filtro de disponibilidade e define o alvo de "a todos" (email E WhatsApp). Sem migração — inclui o critério para decidir se vale a pena criar `employees.city` (0048). **2026-08-20**: junta-se a **pesquisa livre por pessoa** (nome/número) na mesma tabela, a reutilizar `shared/contactSearch.ts` (sem normalizador novo, sem debounce — filtro local); compõe em AND com cidade+disponibilidade e, ao contrário da cidade, **NÃO limpa a seleção** (mostra "N selecionado(s) fora dos filtros atuais" e o alvo do WhatsApp passou a sair da lista completa).
- `whatsapp-integration.md` — **2026-09-10 (NÃO deployado, sem migração): backoffice marca disponibilidade POR um extra (lápis/célula → diálogo, `setForEmployee`, campos partilhados `AvailabilityDayFields`); inbox ordenado pela janela (`sortConversations`: aberta primeiro, a fechar mais cedo no topo; fora da janela por última mensagem), nome do cabeçalho abre a ficha (`useOpenEmployee`, `employeeId` no thread), campo "Semana" do WhatsApp e preview do email preenchidos com a semana selecionada.** **2026-09-09: media entrante (imagens/áudios descarregados da Meta para o storage, colunas `media*` via migração 0065, `InboundMedia` no inbox) + filtros "Ainda não respondeu"/"Sem mensagem nas últimas 24h" na tabela de extras (último contacto DERIVADO de `whatsapp_messages` out + `availability_request_log`, sem tabela nova).** WhatsApp Cloud API + external availability form integration. Full phased plan, Jorge's 5 adjustments + 2 closed semantic decisions, changelogs Fase 0+1 (envs, webhook stub w/ HMAC, mcpApi on Railway, normalizePhoneE164, 3 Drizzle tables + SQL 0045, Graph API client), Fase 2 (listActiveExtras phone, shared runConcurrent, whatsappBroadcast + tRPC whatsapp.sendBroadcast, ExtrasDiaPage WhatsApp dialog — dev mode) Fase 3 (webhook process-then-ack + whatsappInbound parse, whatsappInbox conversations/messages/markRead/reply w/ server-side 24h-window gate, WhatsAppInboxPage w/ 3 window states, invalidEmployeeIds decision) and Fase 4 (single-use JWT form tokens availabilityFormToken/availabilityForm, /api/v1/availability-form/{context,submit} on mcpApi, template URL-button token in broadcast, migration 0047). **INTEGRATION COMPLETE.** 2026-08-04 pós-teste real (broadcast 8): `describeMetaError` com contexto (131030 diz como pôr o número na allowed list; 132001 diz o template + a língua tentados), template `disponibilidade_extras`/`pt_PT` por defeito nos dois dialogs (`shared/whatsappTemplate.ts`), **contrato mudou** (`templateParams[]` → `bodyParam2`: {{1}} = nome do destinatário automático no servidor, {{2}} = campo único do dialog) e o botão do formulário passou a **opt-in** (`includeFormLink`, default OFF — injetá-lo num template sem botão fazia a Meta rejeitar tudo). Inclui a secção "PASSOS MANUAIS NA META". **2026-08-04(b)**: envio guiado pelos METADADOS do template (`server/whatsappTemplateMeta.ts` — `GET /{wabaId}/message_templates`, cache 5 min, env NOVA `WHATSAPP_WABA_ID` com fallback por `/debug_token`): parâmetros NOMEADOS (`{{nome}}`) passam a levar `parameter_name` (fim do erro `(#100) Parameter name is missing`), contagem 0/1/2 adaptada, botão URL detetado automaticamente (manda sobre a checkbox) e validação pré-envio que lista as línguas em que o template EXISTE. Falha de inspeção = fallback silencioso com o motivo anexado ao erro. Includes migration-immutability rule, WhatsApp Manager template setup, and a deploy runbook (migrations 0045+0047, WhatsApp+availability envs). DB engine = MySQL/InnoDB. **2026-08-19 diagnóstico (read-only)**: inbox não recebe respostas porque, na Meta, `GET /{APP_ID}/subscriptions` e `GET /{WABA_ID}/subscribed_apps` estão AMBOS vazios (callback nunca registado + app não subscrita à WABA); endpoint no Vercel verificado OK (verify token local == deploy). Passo WABA `subscribed_apps` EXECUTADO; callback/`messages` na UI = Jorge. Mesmo dia: dialog de broadcast limpo (só Semana + teste + enviar, sem checkbox/dev banner) e **pesquisa de contactos** no inbox (`shared/contactSearch.ts`, filtro local nome/número). **2026-08-20 (NÃO deployado, sem migração)**: **seletor de template** no diálogo da página de disponibilidade — catálogo `WHATSAPP_TEMPLATES` em `shared/whatsappTemplate.ts` com `disponibilidade_extras` (pt_BR, `{{nome}}`+`{{semana}}`) e o NOVO `aviso_de_trabalho` (pt_BR, `{{customer_name}}`+`{{day}}`, campo "Dia" com atalhos para os `dayHeaders` da semana); RULE os parâmetros passam a ser mapeados por **NOME/papel** (`resolveBodyParamRoles`/`orderBodyValues`, resolvidos no SERVIDOR pelo nome do template) com recurso posicional — um template com o dia antes do nome já não sai trocado; **pré-visualização** com o texto REAL da Meta (`whatsapp.templatePreview` + `TemplateAnalysis.bodyText`), nunca uma cópia local. **2026-08-20 (b) fix (NÃO deployado, SEM migração)**: o inbox mostrava bolha VAZIA nos envios de template porque o `sendOne` gravava `body: null` desde a Fase 2 — agora `renderOutboundBody` grava o texto REAL substituído (mesma via da pré-visualização; fallback legível `Template x · nome · dia` quando não há metadados) na coluna `whatsapp_messages.body` que já existia; `messageDisplayBody` é a regra ÚNICA de apresentação (bolha + preview da lista) e as linhas ANTIGAS mostram "Mensagem de template “x” (conteúdo não registado)" — sem back-fill possível.
- `employee-contact-swap.md` — **2026-09-09** script `scripts/swap-employee-phone-nif.ts` (DRY-RUN por defeito, `--apply`) que troca `phone` ↔ `nif` em `employees` quando o NIF começa por 9 ou o telefone não é +351/9; planeador PURO `server/employeeContactSwap.ts` (classificação com checksum de NIF mod-11, telefone estrangeiro nunca vira NIF, só troca quando as DUAS colunas ficam certas, resto vai para REVISÃO), apply transacional com guarda otimista + `activity_logs` `employee_contact_swap`. **APLICADO em prod 09-09 14:28Z: 302 fichas trocadas**; regra depois relaxada (telefone verificado em `nif` troca SEMPRE, lixo vai tal e qual → `*-unverified`) — ⚠️ falta 2.ª passagem `--apply` para 13 fichas. `.env` do dashboard tem `DATABASE_URL` placeholder (exportar o real). `users` não tem NIF/telefone; `driver_applications` fica por curar.
- `pda-checkin-and-shift-handover.md` — **2026-09-09** os dois formulários operacionais dos team leaders: check-in de PDA (`OperationalPage` → `CheckinDialog`; funcionário em `SearchableSelect` com pesquisa, **foto de entrada OBRIGATÓRIA** no zod e na UI) e passagem de turno (`ShiftHandoverPage`; "Número de fardas" substituído por **fardamento com peças/tamanhos/quantidades** — `shared/clothing.ts`, coluna `shift_handovers.clothingItems` JSON via migração 0065, `uniformsCount` mantida e devolvida intacta nos registos antigos, histórico com resumo).
- `rh-auditoria-fase1.md` — **RH fase 1 (set 2026)**: motor de ordenados puro (`server/payroll/`: turnos reais noite/fim-de-semana, dias de contrato, licenças, suspeitos não pagam, testes), `reviewStatus` no ponto + check-in atómico, faltas automáticas PENDENTES até confirmação humana, flags de bloqueio separadas (docs/penalizações/manual), fecho mensal imutável `payroll_runs` (draft→approved→paid), permissões/sanitização de dados pessoais (`rhAccess.ts`), documentos por URL assinada, CSV seguro, classificação de resposta de disponibilidade. Migração 0064.
